기관장급 보고를 위한 \*\*"KDI 대학원 전용 스마트 행정 플랫폼"\*\*의 UI/UX 개선 전략을 제안합니다.

단순히 예쁜 것이 아니라, \*\*"신뢰감(Trustworthy), 전문성(Professional), 정제됨(Refined)"\*\*이라는 키워드에 맞춰 **Tailwind CSS**를 스트림릿에 주입(Injection)하는 방식을 사용하겠습니다.

스트림릿의 투박함을 없애고 모던한 웹사이트처럼 보이게 만드는 **3단계 실행 계획**입니다.

-----

### 🎨 Design Concept: "Institutional Modern"

  * **Color Palette**: KDI의 아이덴티티를 살린 **Deep Navy (\#1e3a8a)**, **Slate Grey (\#64748b)**, 그리고 **Clean White**를 사용합니다.
  * **Layout**: 화면을 꽉 채우는 답답한 테이블 대신, **카드(Card)형 레이아웃**과 \*\*여백(Whitespace)\*\*을 적극 활용합니다.
  * **Typography**: 시스템 기본 폰트 중 가독성이 가장 좋은 산세리프(San-serif) 계열을 강제 적용합니다.

-----

### Step 1. 스타일 로더 모듈 생성 (`utils/style.py`)

먼저 Tailwind CSS를 불러오고, 스트림릿의 기본 디자인(상단 여백, 투박한 폰트 등)을 덮어씌우는 모듈을 만듭니다.

**파일 생성:** `utils/style.py`

```python
import streamlit as st

def load_css():
    """
    Tailwind CSS CDN을 로드하고 KDI 대학원 스타일에 맞는 커스텀 CSS를 적용합니다.
    """
    st.markdown("""
        <script src="https://cdn.tailwindcss.com"></script>
        
        <style>
            /* 1. 기본 폰트 및 배경 설정 */
            @import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');
            
            html, body, [class*="css"] {
                font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, Roboto, sans-serif !important;
            }
            
            /* 전체 배경색: 아주 연한 회색 (Slate-50)으로 설정하여 눈의 피로 감소 */
            .stApp {
                background-color: #f8fafc; 
            }

            /* 2. 스트림릿 상단 헤더 숨김 (깔끔하게) */
            header[data-testid="stHeader"] {
                visibility: hidden;
            }
            
            /* 3. 메인 컨테이너 패딩 조절 */
            .main .block-container {
                padding-top: 2rem;
                padding-bottom: 5rem;
                max-width: 1200px;
            }

            /* 4. 사이드바 스타일링 */
            section[data-testid="stSidebar"] {
                background-color: #ffffff;
                border-right: 1px solid #e2e8f0;
            }
            
            /* 5. 커스텀 카드 스타일 (HTML 렌더링용) */
            .kdi-card {
                background-color: white;
                border-radius: 0.75rem;
                padding: 1.5rem;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
                border: 1px solid #f1f5f9;
                height: 100%;
                transition: transform 0.2s ease-in-out;
            }
            .kdi-card:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            }
            
            .kdi-header {
                color: #1e3a8a; /* KDI Navy */
                font-weight: 700;
                letter-spacing: -0.025em;
            }
            
            .kdi-metric-label {
                color: #64748b;
                font-size: 0.875rem;
                font-weight: 500;
            }
            
            .kdi-metric-value {
                color: #0f172a;
                font-size: 1.875rem;
                font-weight: 700;
            }
        </style>
    """, unsafe_allow_html=True)

def card_metric(label, value, diff=None, icon="📊"):
    """
    Tailwind 스타일이 적용된 HTML 메트릭 카드를 반환합니다.
    """
    diff_html = ""
    if diff:
        color = "text-green-600" if "+" in diff else "text-red-600"
        diff_html = f'<span class="text-sm {color} font-medium ml-2">{diff}</span>'
        
    return f"""
    <div class="kdi-card">
        <div class="flex items-center justify-between mb-2">
            <span class="text-2xl">{icon}</span>
        </div>
        <div class="kdi-metric-label">{label}</div>
        <div class="flex items-end">
            <div class="kdi-metric-value">{value}</div>
            {diff_html}
        </div>
    </div>
    """

def page_header(title, subtitle):
    """
    페이지 상단 헤더 컴포넌트
    """
    st.markdown(f"""
    <div class="mb-8 border-b border-slate-200 pb-6">
        <h1 class="text-3xl font-bold text-slate-900 tracking-tight">{title}</h1>
        <p class="text-slate-500 mt-2 text-lg">{subtitle}</p>
    </div>
    """, unsafe_allow_html=True)
```

-----

### Step 2. 메인 페이지 (`main.py`) 전면 개편

기존의 텍스트 나열형 메인 페이지를 **"Executive Summary 대시보드"** 형태로 바꿉니다.

**수정:** `main.py`

```python
import streamlit as st
from utils.style import load_css, page_header, card_metric
from utils.github_handler import load_data

# 1. 페이지 설정
st.set_page_config(
    page_title="KDI 대학원 스마트 행정 플랫폼",
    page_icon="🏛️",
    layout="wide",
    initial_sidebar_state="expanded"
)

# 2. Tailwind CSS 및 스타일 로드
load_css()

# 3. 데이터 로드 (요약 정보 표시용)
dash_data = load_data("dashboard_data.json")

# 4. 메인 헤더
page_header(
    title="KDI School Smart Platform", 
    subtitle="AI 기반 스마트 행정 지원 시스템에 오신 것을 환영합니다."
)

# 5. 핵심 KPI 요약 (카드 형태) - HTML/Tailwind로 렌더링
kpi = dash_data.get("kpi", {"total_students": 0, "partners": 0, "employment_rate": 0})

col1, col2, col3, col4 = st.columns(4)

with col1:
    st.markdown(card_metric("총 재학생 수", f"{kpi['total_students']}명", "+12%", "🎓"), unsafe_allow_html=True)
with col2:
    st.markdown(card_metric("글로벌 파트너", f"{kpi['partners']}개", "+3", "🌍"), unsafe_allow_html=True)
with col3:
    st.markdown(card_metric("취업률", f"{kpi['employment_rate']}%", "+1.5%", "📈"), unsafe_allow_html=True)
with col4:
    st.markdown(card_metric("오늘의 일정", "5건", None, "📅"), unsafe_allow_html=True)

st.markdown('<div class="h-8"></div>', unsafe_allow_html=True) # 여백

# 6. 주요 기능 바로가기 (Quick Access)
st.markdown("""
<h2 class="text-xl font-bold text-slate-800 mb-4">Quick Access</h2>
""", unsafe_allow_html=True)

col_a, col_b, col_c = st.columns(3)

# 기능 카드 1
with col_a:
    st.markdown("""
    <div class="kdi-card group cursor-pointer hover:border-blue-500 transition-colors">
        <div class="flex items-center space-x-3 mb-3">
            <div class="bg-blue-100 p-2 rounded-lg text-blue-600">🤖</div>
            <h3 class="font-bold text-slate-800 text-lg">주간보고 AI 챗봇</h3>
        </div>
        <p class="text-slate-500 text-sm leading-relaxed mb-4">
            지난 주간보고서를 AI가 분석하여 주요 이슈와 일정을 즉시 답변해 드립니다.
        </p>
        <div class="text-blue-600 text-sm font-semibold group-hover:translate-x-1 transition-transform inline-flex items-center">
            바로가기 →
        </div>
    </div>
    """, unsafe_allow_html=True)
    if st.button("AI 챗봇 실행", key="btn_chatbot", use_container_width=True):
        st.switch_page("pages/2_주간보고_AI_챗봇.py")

# 기능 카드 2
with col_b:
    st.markdown("""
    <div class="kdi-card group">
        <div class="flex items-center space-x-3 mb-3">
            <div class="bg-indigo-100 p-2 rounded-lg text-indigo-600">👥</div>
            <h3 class="font-bold text-slate-800 text-lg">직원/전문가 추천</h3>
        </div>
        <p class="text-slate-500 text-sm leading-relaxed mb-4">
            업무 키워드로 교내 최적의 협업 파트너와 전문가를 찾아 매칭합니다.
        </p>
        <div class="text-indigo-600 text-sm font-semibold group-hover:translate-x-1 transition-transform inline-flex items-center">
            바로가기 →
        </div>
    </div>
    """, unsafe_allow_html=True)
    if st.button("직원 검색 실행", key="btn_staff", use_container_width=True):
        st.switch_page("pages/4_직원_추천_시스템.py")

# 기능 카드 3
with col_c:
    st.markdown("""
    <div class="kdi-card group">
        <div class="flex items-center space-x-3 mb-3">
            <div class="bg-emerald-100 p-2 rounded-lg text-emerald-600">📋</div>
            <h3 class="font-bold text-slate-800 text-lg">기관평가 코칭</h3>
        </div>
        <p class="text-slate-500 text-sm leading-relaxed mb-4">
            평가 지표별 가이드라인과 전년도 피드백을 분석하여 전략을 제시합니다.
        </p>
        <div class="text-emerald-600 text-sm font-semibold group-hover:translate-x-1 transition-transform inline-flex items-center">
            바로가기 →
        </div>
    </div>
    """, unsafe_allow_html=True)
    if st.button("평가 코칭 실행", key="btn_eval", use_container_width=True):
        st.switch_page("pages/5_기관평가_코칭.py")

# 7. 하단 안내
st.markdown("---")
st.markdown("""
<div class="text-center text-slate-400 text-sm py-4">
    © 2025 KDI School of Public Policy and Management. All rights reserved.<br>
    Powered by <strong>UStudio AI Solutions</strong>
</div>
""", unsafe_allow_html=True)
```

-----

### Step 3. 다른 페이지에도 스타일 적용하는 법

모든 `pages/*.py` 파일의 최상단에 다음 코드를 추가하면 통일된 디자인이 적용됩니다.

```python
import streamlit as st
from utils.style import load_css, page_header # utils/style.py에서 임포트

st.set_page_config(layout="wide")
load_css() # Tailwind 및 커스텀 스타일 주입

# 기존 st.title 대신 커스텀 헤더 사용
page_header("주간보고 AI 챗봇", "업무 보고서 데이터를 기반으로 질문에 답변합니다.")

# ... 나머지 코드 ...
```

-----

### ✅ 개선 포인트 요약 (기관장 보고용)

1.  **시각적 안정감**: 눈이 아픈 쨍한 흰색 대신 `Slate-50` 톤의 배경과 카드 UI를 적용하여 **"보고서"가 아닌 "플랫폼"** 느낌을 줍니다.
2.  **명확한 정보 위계**: 중요한 숫자(KPI)는 크게, 부가 설명은 작고 흐리게 처리하여 **의사결정권자가 봐야 할 정보**를 강조했습니다.
3.  **브랜드 아이덴티티**: KDI의 이미지를 연상시키는 블루/네이비 컬러를 포인트로 사용하여 **소속감**을 줍니다.
4.  **사용자 경험(UX)**: 텍스트 링크 대신 박스 형태의 '카드'를 배치하여 클릭 영역을 넓히고 직관성을 높였습니다.

지금 바로 `utils/style.py`를 만들고 `main.py`를 위 코드로 교체해 보세요. 앱의 분위기가 완전히 달라질 것입니다.